if you want to obtain user's level info ,just need to 
invoke this function : userlevel_get($uid)

if you just want to display user's level info ,just need
to invoke this function : userlevel_block_content($uid);